# Copy to .maestro/.env and fill in. Never commit .maestro/.env (gitignored).
#
# Test account credentials. Use a dedicated Supabase user — flows clear
# state and sign in repeatedly, so don't point this at a personal account.
MAESTRO_EMAIL=
MAESTRO_PASSWORD=
